export const UNIT_SIZE = 8
export const UNIT_SIZE_HALF = UNIT_SIZE / 2
export const MS = 50
export const RADIUS = UNIT_SIZE_HALF * 0.95
export const MAX_WIDTH = UNIT_SIZE * 100
export const MAX_HEIGHT = UNIT_SIZE * 9
export const SPACER = [0, 0, 0, 0, 0, 0, 0, 0]
